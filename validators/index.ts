/**
 * `@kin-form/validators` — common validator factories (`required`,
 * `minLength`, `maxLength`, `min`, `max`, `url`, `email`, `pattern`,
 * `maxFileSize`, `password`) plus `toSchemaValidator()`, a whole-group/whole-form
 * adapter for any Standard Schema-compliant library.
 *
 * @module
 */

export * from "./validators.ts";
export * from "./schema.ts";
